# ===========================================
# Spring Boot JWT Authentication System
# 환경별 설정 가이드
# ===========================================

# ===== Spring Profile 설정 =====
# 개발환경: dev, 운영환경: prod
SPRING_PROFILES_ACTIVE=dev

# ===== 데이터베이스 설정 =====
# 개발환경 (Docker 컨테이너)
DB_USERNAME=your_username
DB_PASSWORD=your_password

# 운영환경 (외부 DB 서버) - prod 프로파일에서만 사용
DB_HOST=localhost
DB_PORT=5432
DB_NAME=LoginProject

# ===== Redis 설정 =====
# 개발환경 (Docker 컨테이너)
REDIS_PASSWORD=your_redis_password

# 운영환경 (외부 Redis 서버) - prod 프로파일에서만 사용
REDIS_HOST=localhost
REDIS_PORT=6379

# ===== JWT 보안 설정 =====
JWT_SECRET_KEY=your-jwt-secret-key-min-256-bits
JWT_ACCESS_EXPIRATION=3600000    # Access Token: 1시간 (밀리초)
JWT_REFRESH_EXPIRATION=86400000  # Refresh Token: 24시간 (밀리초)

# ===== Swagger 문서화 설정 =====
# 개발환경: 기본값 사용 (http://localhost:8080)
# 운영환경: 실제 도메인으로 변경 필요
# SWAGGER_SERVER_URL=https://api.yourdomain.com

# Google OAuth2
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Naver OAuth2
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret
NAVER_REDIRECT_URI=http://localhost:8080/login/oauth2/naver

# Kakao OAuth2
KAKAO_CLIENT_ID=your_kakao_client_id
KAKAO_CLIENT_SECRET=your_kakao_client_secret
KAKAO_REDIRECT_URI=http://localhost:8080/login/oauth2/kakao